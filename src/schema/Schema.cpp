#include "ionet/schema/Schema.h"
#include "ionet/schema/SchemaBuilder.h"

// Schema implementation is header-only for now.
// This file exists to satisfy the build system and
// will contain implementations as complexity grows.

namespace ionet::schema {

// Future implementations:
// - Schema serialization
// - Schema validation helpers
// - Schema merging

} // namespace ionet::schema